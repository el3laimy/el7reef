enum RosterAction {
  moveToStarter,
  moveToBench,
  makeInactive,
  promoteViceCaptain,
  demoteToPlayer,
  markAvailable,
  markUnavailable,
  markInjured,
  remove,
  shareGuestClaim,
}

enum TemplateAction { apply, delete }
