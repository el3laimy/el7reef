<div dir="rtl">

# الخطة الأم لتطوير الحريف وصندوق الأفكار

**الإصدار:** 1.0

**الحالة:** `ACTIVE` — المرجع الوحيد للأولوية والحالة والتنفيذ

**تاريخ خط الأساس:** 28 يوليو 2026

**المنتج:** نواة مجد البطولات — `Tournament Ego MVP`&rlm;

**المنصة:** Android فقط؛ لا أعمال أو اختبارات أو إعدادات تخص iOS

**Firebase:** المشروع `el7reef-app`&rlm;، قاعدة `(default)`&rlm;، إصدار Standard، المنطقة `nam5`&rlm;

---

## 1. سلطة الوثيقة وطريقة استخدامها

هذه الوثيقة هي المرجع التنفيذي الوحيد للمشروع. تحفظ الرؤية، الحقيقة الحالية، تذاكر الإصدار النشطة، خارطة ما بعد الإصدار، صندوق الأفكار، والقرارات المؤجلة أو المرفوضة. لا تُفتح مهمة من خطة قديمة أو تقرير تاريخي مباشرة؛ يجب أن تحصل أولًا على معرّف وحالة ومعيار قبول هنا.

تظل المراجع المتخصصة التالية مصادر صحيحة في مجالها، لكنها لا تحدد الأولوية أو حالة التنفيذ:

- يحفظ الملف `PRODUCT.md`&rlm; اتجاه المنتج ووعده الأساسي.
- يحفظ الملف `DESIGN.md`&rlm; نظام Daylight Street Glass والقرارات البصرية.
- تحفظ الوثائق `01` إلى `04`&rlm; متطلبات المنتج والبرنامج والمعمارية وتجربة الاستخدام.
- تحفظ Runbooks ‏`11` إلى `13`&rlm; إجراءات الترحيل والخصوصية وGoogle Play&rlm;.
- تحفظ ADRs والمخططات المتخصصة العقود المعمارية؛ أي تعارض مع الحقيقة الحالية يفتح تذكرة أو ADR جديدة بدل إنشاء Roadmap موازية.
- يحفظ `docs/archive/PLANNING_RETIREMENT_MANIFEST.md`&rlm; مسارات الاستعادة فقط، ولا يحمل أولوية أو حالة حية.

### 1.1 خريطة القراءة السريعة

| السؤال | القسم الحاكم |
| :--- | :--- |
| ما الذي يمنع إطلاق Android V1 الآن؟ | لوحة V1 النشطة وموجات التنفيذ |
| ما الذي سيأتي بعد الإصدار؟ | خارطة V1.1 وV2 |
| أين توضع فكرة جديدة؟ | صندوق الأفكار بحالة `INBOX`&rlm; |
| لماذا جُمّدت أو رُفضت فكرة؟ | سجل القرارات |
| ما الدليل المطلوب للإغلاق؟ | عقد التذكرة وتعريف الاكتمال |
| أين توجد الخطة القديمة؟ | سجل التقاعد وGit tag الموثق فيه |

## 2. الرؤية والجمهور والثوابت

### 2.1 الرؤية

الحريف يحول بطولة الشارع من تنظيم متفرق إلى تجربة رسمية قابلة للتشغيل والمشاركة: بطولة تنتج مباريات، والمباريات تنتج بيانات موثوقة، والبيانات تنتج لحظات فخر، والمشاركة تجلب لاعبين جدد يعودون للمطالبة بهوياتهم ومتابعة البطولة.

الجمهور الأول هو منظم البطولة الذي يعمل من هاتف Android في ظروف ملعب حقيقية، يليه قائد الفريق، واللاعب المسجل، واللاعب الزائر الذي لا يملك حسابًا بعد. يجب ألا يتوقف التشغيل بسبب غياب حساب أو تشكيلة.

### 2.2 الثوابت غير القابلة للتفاوض

- يكون `TournamentParticipant`&rlm; مرجع المشارك داخل البطولة.
- يكون `MatchEvent`&rlm; مصدر الحقيقة للأهداف وMVP وإحصائيات المباراة، لكل أنواع اللاعبين.
- تستخدم أحداث المباراة والعقود الإحصائية `ParticipantRef`&rlm; الحالي بدل افتراض لاعب مسجل فقط؛ اسم `PlayerIdentityRef` الوارد في وثائق أقدم كان تصميمًا مستهدفًا ولم يصبح رمزًا في الكود.
- تحصل الفرق واللاعبون الزوار على التشغيل والإحصائيات وبطاقات المفاخرة ومسار المطالبة نفسه.
- تظل التشكيلة اختيارية؛ يظهر تشجيع واضح ولا تُمنع بداية المباراة بسبب غيابها.
- لا تؤثر نتيجة في الترتيب أو الشجرة أو البطل قبل الاعتماد الرسمي.
- تنفذ الخدمات والـBackend العمليات الحساسة؛ المتحكمات تنسق ولا تقرر السلطة أو تكتب الحقيقة مباشرة.
- تكون العمليات الحساسة ذرية، قابلة لإعادة المحاولة بأمان، وتنتج Audit موثوقًا من داخل العملية نفسها.
- لا توجد واجهة وهمية أو زر ميت أو تحليل تكتيكي أو رقم لم يُشتق من بيانات حقيقية.
- تظل Fantasy وSocial المتقدم خارج V1، ولا توجد أعمال أو اختبارات أو إعدادات تخص iOS.

### 2.3 مؤشرات المنتج الحاكمة

- قياس زمن إنشاء بطولة بسيطة وإضافة أول فريق ولاعب.
- قياس زمن تسجيل خمسة أهداف وMVP وفتح المشاركة.
- قياس نجاح روابط WhatsApp وQR وClaim ونسب fallback&rlm;.
- قياس الاعتماد دون نزاع أو فقد بيانات، وأول بطاقة مفاخرة بعد لحظة الإنجاز.
- عدم تسمية الرجوع من Android share sheet مشاركة مكتملة.

## 3. عقد التذاكر والأفكار

### 3.1 المعرّفات

تستخدم العناصر معرّفًا ثابتًا من الشكل `ELR-{DOMAIN}-{NNN}`&rlm;. المجالات المعتمدة هي:

| المجال | الاستخدام |
| :--- | :--- |
| `SEC` | الأمن والخصوصية والثقة والصلاحيات |
| `CORE` | البطولة والمباراة والتشكيلة والبيانات التشغيلية |
| `UX` | تجربة الاستخدام والوصول والنظام البصري |
| `GROW` | المشاركة وClaim والاستكشاف والنمو |
| `IDN` | هوية اللاعب والفريق والمصداقية |
| `TEAM` | العضويات والانضمام وإدارة الفرق |
| `NOTIF` | الإشعارات وصندوق الوارد |
| `OPS` | Backend وCI والاعتماديات والقياس |
| `REL` | Firebase وAndroid وPlay والإطلاق |
| `FANT` | Fantasy المؤجلة |
| `SOC` | Social المؤجل |
| `BIZ` | فرص الأعمال والخرائط والملاعب |

### 3.2 الأفق والحالة

- يكون الأفق واحدًا من `V1`, `V1.1`, `V2`, `LATER`, `NONE`&rlm;.
- تكون الحالة واحدة من `INBOX`, `READY`, `ACTIVE`, `BLOCKED`, `DONE`, `PARKED`, `REJECTED`, `DUPLICATE`&rlm;.
- تحمل كل تذكرة: الأولوية، المالك، التبعيات، المصدر، الأثر على البيانات والأمن، الاختبارات، معيار القبول، والدليل.
- لا تنتقل فكرة إلى `ACTIVE` قبل اكتمال Definition of Ready&rlm;.
- تحمل `PARKED` سبب التأجيل وشرط إعادة الفتح، وتحمل `REJECTED` سبب القرار، وتحمل `DONE` دليلًا قابلًا للإعادة.
- تظل المعرفات القديمة aliases في سجل التقاعد ولا تستخدم لفتح عمل جديد.

### 3.3 Definition of Ready

تكون التذكرة جاهزة فقط عند تحديد المشكلة والمستخدم المتأثر، الخدمة المالكة، مصدر البيانات والكتابة، الصلاحيات والذرية والتوافق، التبعيات، الاختبارات ومعيار القبول، والمالك المسؤول عن الدليل.

### 3.4 Definition of Done

لا يكفي وجود الكود. يلزم مرور التحليل والاختبارات المناسبة، التحقق ببيانات حقيقية، توثيق السلوك والفشل والـrollback، وربط الدليل بالـcommit أو تقرير Emulator أو الجهاز أو Play Console بحسب نوع التذكرة.

## 4. القرار التنفيذي لإطلاق V1

الإصدار غير جاهز للنشر رغم اكتمال أجزاء كبيرة من تجربة البطولة والتصميم. بوابة الإصدار الجديدة هي **سلامة السلطة والبيانات أولًا**؛ فلا تُنشر Cloud Functions الحالية، ولا يبدأ Closed Testing، قبل إغلاق ثغرات التدقيق والتصويت والنزاعات وبيانات اللاعبين وإعادة CI إلى الحالة الخضراء.

تظل حلقة المنتج المستهدفة كما هي:

> إنشاء بطولة ← فرق ولاعبون مسجلون وضيوف ← مباراة وتشكيلة اختيارية ← أهداف وMVP وترجيح ← اعتماد ذري ← مجموعات وشجرة ← Pride Share ← Claim ← عودة إلى البطولة.

ترتيب التنفيذ الملزم:

`احتواء فوري → Backend موثوق → فصل الملف العام والخاص → UX المتبقي وCI → نشر Firebase → AAB وClosed Testing → Pilot وتدرج الإطلاق`

لا تدخل Fantasy أو Social المتقدم أو iOS أو إعادة تصميم بصرية عامة جديدة في موجة V1 النشطة، لكنها تحفظ في صندوق الأفكار بحالتها الصحيحة.

---

## 5. الحقيقة الحالية ومصادر العمل

### 5.1 الحالة المثبتة في 28 يوليو 2026

| البند | الحالة المثبتة | القرار |
| :--- | :--- | :--- |
| Git | الفرع `main`&rlm;، الرأس `8799959`&rlm;، وworking tree نظيف قبل إنشاء هذه الخطة | خط أساس التنفيذ |
| Firestore | `(default)` Standard في `nam5` داخل `el7reef-app`&rlm; | لا تغيير للاسم أو المنطقة أو الإصدار |
| Cloud Functions | `functions:list` أعاد قائمة فارغة | الـcallable غير الآمنة ليست مكشوفة إنتاجيًا الآن، لكنها تمنع أي نشر قادم |
| Android signing | لا يوجد `android/key.properties` ولا upload keystore محلي | Release AAB ينتظر OWNER |
| Android QA | AVD باسم `Pixel_4a_API_35_Lite` موجود، ولا يوجد جهاز Android متصل وقت المراجعة | يُعاد تشغيله عند موجة QA |
| CI | root lockfile موجود محليًا لكنه ignored وغير متتبع | clean checkout يفشل قبل الاختبارات |
| Hotspots | البوابة تفشل في 7 ملفات، لا 6 | ادعاء الخطة القديمة أصبح قديمًا |
| الاعتماديات | `firebase-admin 13.10.0` و`firebase-functions 6.6.0` و`fast-xml-parser 5.9.3`&rlm; | يلزم audit متصل جديد قبل الحكم النهائي |

### 5.2 تصنيف مصادر العمل وقت الدمج

| المصدر | التصنيف من الآن | ما يُحفظ منه |
| :--- | :--- | :--- |
| خطة V1 الموحدة السابقة | `SUPERSEDED` | القرارات المعمارية استوعبت هنا، والنص الأصلي في سجل التقاعد |
| لقطة التقدم حتى 14 يوليو | `HISTORICAL EVIDENCE` | الأدلة القديمة داخل `docs/archive/evidence`، لا أعداد التذاكر الحالية |
| إعادة تصميم التشكيلة السابقة | `DONE` مع بوابة regression | `ELR-UX-802` منجز؛ المشكلة الجديدة في `ELR-UX-301` |
| سجل الدين المعماري السابق | `SUPERSEDED` | المتبقي الصحيح في `ELR-OPS-401..404` و`ELR-REL-502` |
| خطط الجذر وTOS والـbacklogs القديمة | `MERGED/PARKED/REJECTED` | الـCrosswalk الكامل في سجل التقاعد؛ لا تُستخدم كـbacklog موازية |
| Runbooks الخصوصية وPlay | قوائم قبول تابعة لهذه الخطة | تُغلق داخل موجة الإصدار فقط |

أي `Todo` داخل خطة مستبدلة لا يصبح عملًا مطلوبًا تلقائيًا. يُنفذ فقط ما يحمل معرفًا في هذه الوثيقة.

---

## 6. حكم التقرير الأمني

مرجع التدقيق القابل للآلة: `docs/audits/2026-07-28_v1_security_completion_audit.json`&rlm;. نتيجة النطاق الحالي **1/5** بسبب تسريب بيانات شخصية موثق وغياب سلطة موثوقة لعدد من العمليات.

| المعرف | الحكم | الخطورة | الملاحظة الدقيقة |
| :--- | :---: | :---: | :--- |
| `SEC-AUDIT-001` | صحيح في الكود المرشح | حرج | `recordAuditEvent` يثبت هوية الفاعل والوقت فقط، لكنه يثق في الكيان والإجراء والـpayload. لا توجد Functions منشورة الآن، لذلك الخطر حاجز نشر مباشر لا callable إنتاجية عاملة حاليًا |
| `SEC-VOTE-001` | صحيح | عالٍ | transaction العميل لا يمكن أن تكتمل لأن rules تمنع تحديث `fanVotingSessions`&rlm;؛ كما أن إنشاء `userVotes` ضعيف التحقق |
| `SEC-DISPUTE-001` | صحيح | حرج | كل مستخدم مسجل يقرأ كل النزاعات، ويمكنه إنشاء نزاع باسمه على مباراة لا تخصه وبحقول غير مقيدة |
| `SEC-DISPUTE-002` | صحيح | عالٍ | الإنشاء/الإغلاق والتجميد/الفك ليست ذرية، و`pendingReview` مرفوضة في مسار تحديث المنظم الحالي |
| `SEC-PLAYER-001` | صحيح | عالٍ | أول إنشاء لـ`players/{uid}` يسمح بتزوير rating/counters/trust/role/createdAt وحقول إضافية |
| `SEC-PRIVACY-001` | صحيح | حرج | مستند اللاعب المقروء لكل المسجلين يحتوي الهاتف وقوائم الصداقة والمتابعة والحظر؛ `privacySetting` لا تحمي القراءة |
| `CI-LOCK-001` | صحيح مع تصحيح | عالٍ | root lock غير متتبع، والـhotspots الفاشلة 7 لا 6 |
| `DEP-AUDIT-001` | غير مثبت كرقم حالي | يحتاج إعادة فحص | لا نعتمد رقم 10 قبل `npm audit` متصل ومؤرخ. الإصدار المثبت من `fast-xml-parser` هو 5.9.3، بينما التنبيه High المحدد [`GHSA-8gc5-j5rx-235r`](https://github.com/advisories/GHSA-8gc5-j5rx-235r) يصيب إصدارات أقل من 5.5.6، لذلك لا يثبت وحده أن 5.9.3 مصاب |

أحداث التدقيق القديمة التي لا تحمل provenance موثوقًا لا يجوز عرضها بوصفها «سجلًا رسميًا». تُقرأ كـ`legacyUnverified` أو تُخفى من العرض الرسمي، ولا تُرقّى جماعيًا إلى موثوقة.

---

## 7. العقود العامة المطلوبة

### 7.1 التدقيق

- حذف export العام `recordAuditEvent`&rlm;.
- إضافة كاتب داخلي غير مُصدّر `appendAuditEvent({transaction, entityType, entityId, action, actorId, beforePayload, afterPayload, metadata, requestId})`&rlm;.
- يشتق Backend الإجراء والكيان وbefore/after من المستندات المقروءة داخل transaction، ولا يقبلها كحقيقة من العميل.
- الحدث الموثوق يحمل `source: trustedOperation` و`verificationVersion: 1` و`requestId` ووقت الخادم.

### 7.2 التصويت

- Callable واحدة: `castFanVote({matchId, candidateRef, requestId})`&rlm;؛ لا يوجد `userId` في الطلب.
- `candidateRef` يستخدم عقد `ParticipantRef` الحالي بالقيم `player` أو `guestPlayer` أو `matchSidePlayer` مع `id` فقط كهوية قرار؛ الاسم لا يُوثق من العميل.
- يشتق المصوّت من Auth، ويتحقق الخادم من الجلسة ووقت الخادم والمرشح والـroster الرسمية وعمر الحساب الموثوق وعدم مشاركة المصوّت وعدم وجود vote حتمي سابق باسم `${matchId}_${uid}`&rlm;.
- جلسة التصويت تُنشأ أثناء settlement الموثوق وتُغلق أثناء approval أو job موثوق. كل الكتابات المباشرة إلى `fanVotingSessions` و`userVotes` ممنوعة.
- الضيوف مؤهلون للفخر والتصويت؛ أي rating bonus يظل للاعب المسجل فقط.

### 7.3 النزاعات

- `openMatchDispute({matchId, type, description, evidencePaths, requestId})`&rlm;؛ `type` من enum الحالية، والوصف بين 10 و1000 حرف، والأدلة ثلاثة مسارات خاصة كحد أقصى وليست روابط تنزيل عامة.
- `decideMatchDispute({disputeId, decision, resolutionNote, requestId})` حيث `decision` واحدة من `upholdResult` أو `reopenForCorrection`&rlm;.
- يفتح النزاع مستخدم مسجل مشارك فعليًا في roster، أو لاعب ضيف تم ربطه بهذا الحساب، أو منظم المباراة. الحسم للمنظم فقط في V1.
- transaction الفتح تنشئ النزاع، تحفظ `matchStatusBeforeFreeze`، تضع `activeScoreDisputeId` على المباراة، تجمد نزاع النتيجة، وتكتب audit واحدة.
- `upholdResult` يغلق النزاع كـ`rejected` ويعيد الحالة السابقة؛ `reopenForCorrection` يغلقه كـ`resolved` وينقل المباراة إلى `pendingReview`؛ كلاهما يمسح marker ويكتب audit داخل transaction نفسها.
- `requestId` يجعل retry idempotent، وmarker يمنع سباق نزاعين مفتوحين من النوع نفسه.
- تُحفظ الأدلة تحت مساحة Storage خاصة بالنزاع، وتسمح قواعد Storage بالرفع والقراءة للرافع وبالقراءة لمنظم المباراة فقط. يقرأ الكود `evidenceUrls` القديمة كـlegacy ولا يعيد نشرها، بينما كل نزاع جديد يستخدم `evidencePaths`&rlm;.

### 7.4 الحساب والملف العام

- `ensurePlayerProfile()` بلا payload موثوق؛ يشتق UID وبيانات Auth المسموحة ويكتب defaults والوقت من الخادم.
- `updateOwnPlayerProfile({name, photoUrl, photoThumbUrl, photoFrame, position, privacySetting})` يحدّث الحقول القابلة للتحرير فقط.
- `setUsername({username, requestId})` يحجز الاسم ويحدث الملف الخاص والإسقاط العام ذريًا.
- يبقى `players/{uid}` هو مستند الحساب الخاص: المالك يقرأه، والـBackend يكتب الحقول الموثوقة، ولا يوجد client create بعد cutover.
- يضاف `playerPublicProfiles/{uid}` كإسقاط Backend-only. حقوله القصوى: `name`, `nameLower`, `username`, `usernameLower`, `photoUrl`, `photoThumbUrl`, `photoFrame`, `position`, `rating`, `totalMatches`, `wins`, `draws`, `losses`, `mvpCount`, `trustLevel`, `achievementIds`, `privacySetting`, `updatedAt`&rlm;.
- يُمنع تمامًا وجود `phone`, `friendIds`, `followingIds`, `blockedIds`, `trustWeight`, `role`, `createdAt` أو claim data في الإسقاط العام.
- عند `privacySetting != public` يحتفظ الإسقاط بالهوية الدنيا اللازمة للـroster فقط ويزيل الصورة والإحصاءات؛ يعرض التطبيق «هذا الحساب خاص». دعم friends-only الكامل يؤجل إلى Social V2.

---

## 8. لوحة V1 النشطة وموجات التنفيذ

### 8.1 سجل الحالة الحي

| المعرفات | الأفق | الحالة | الأولوية | المالك | التبعية | المصدر والدليل الحالي |
| :--- | :---: | :---: | :---: | :---: | :--- | :--- |
| `ELR-SEC-001..004` | `V1` | `READY` | `P0` | ENG | لا يوجد | التدقيق الأمني المؤرخ؛ aliases ‏`V3-001..004` |
| `ELR-SEC-101..104` | `V1` | `BLOCKED` | `P0` | ENG | بوابة W0 | العقود الأمنية في القسم 7؛ aliases ‏`V3-101..104` |
| `ELR-IDN-201..205` | `V1` | `BLOCKED` | `P0` | ENG | بوابة W1 | تدقيق الخصوصية وخطة cutover؛ aliases ‏`V3-201..205` |
| `ELR-UX-301..304` | `V1` | `READY` | `P1` | ENG + QA | تثبيت build حديث؛ الأمن يسبق النشر | صور المستخدم والتدقيق الحالي؛ aliases ‏`V3-301..304` |
| `ELR-OPS-401..402` | `V1` | `READY` | `P0` | ENG | لا يوجد | CI وHotspot audit؛ aliases ‏`V3-401..402` |
| `ELR-OPS-403..404` | `V1` | `BLOCKED` | `P0/P1` | OWNER + ENG | موافقة الشبكة ثم نتيجة التدقيق | aliases ‏`V3-403..404` |
| `ELR-REL-501..507` | `V1` | `BLOCKED` | `P0` | OWNER + ENG + QA | بوابات W1 وW2 وW4 | aliases ‏`V3-501..507` |

الحالة الحالية لا تدّعي بدء تنفيذ أي تذكرة منتج جديدة أثناء عملية توحيد الوثائق. عند بدء العمل يتغير صف التذكرة المحددة فقط إلى `ACTIVE`، ولا تُستخدم حالة الموجة بدل حالة العنصر.

### Wave 0 — الاحتواء وإعادة خط الأساس `P0`

| التذكرة | التنفيذ | القبول |
| :--- | :--- | :--- |
| `ELR-SEC-001` (`V3-001`) | إضافة `fan_voting_enabled=false` و`disputes_enabled=false` محليًا، وإخفاء مداخل الشاشتين دون dead route | لا يمكن للمستخدم الوصول إلى مسار كتابة مكسور؛ لا نشر Remote Config قبل مراجعة OWNER |
| `ELR-SEC-002` (`V3-002`) | إزالة callable العامة للتدقيق واستبدال اختبارات نجاحها باختبار رفض/غياب export | entrypoint لا يصدر `recordAuditEvent` وأي client direct write مرفوض |
| `ELR-SEC-003` (`V3-003`) | قواعد احتواء مستقلة: منع dispute/vote/session direct writes، وتقييد player create مؤقتًا بمخطط وقيم defaults دقيقة حتى جاهزية `ensurePlayerProfile` | attack tests تمنع forged role/rating/createdAt والحقول الزائدة، مع بقاء تسجيل الدخول الحالي صالحًا مؤقتًا |
| `ELR-SEC-004` (`V3-004`) | تحديث واجهة timeline لتمييز الأحداث بلا provenance كـ«قديم غير موثق» | لا يظهر حدث قديم كإجراء رسمي موثوق |

**بوابة W0:** قواعد الاحتواء 5/5 في التدقيق المحدود، وكل اختبارات Auth الحالية تمر قبل أي نشر.

### Wave 1 — العمليات الموثوقة والذرية `P0`

| التذكرة | التنفيذ | القبول |
| :--- | :--- | :--- |
| `ELR-SEC-101` (`V3-101`) | كاتب audit داخلي داخل معاملات settlement/approval/claim/moderation والحساب | لا توجد API عامة لإنشاء حدث رسمي؛ failure يَفشل العملية كاملة أو يتبع سياسة صريحة لكل عملية |
| `ELR-SEC-102` (`V3-102`) | `castFanVote` والـrepository/controller العميل كواجهة callable فقط | تصويت صحيح يزيد مرة واحدة، وduplicate/مشارك/حساب حديث/مرشح خارج roster/جلسة مغلقة تُرفض بلا كتابة |
| `ELR-SEC-103` (`V3-103`) | `openMatchDispute` و`decideMatchDispute` ومعاملات الحالة المحددة أعلاه | failure injection وretry والتزامن لا تترك dispute أو match نصف مكتملة |
| `ELR-SEC-104` (`V3-104`) | Rules وindexes للقراءة: `userVotes` لصاحب الصوت فقط بينما تعرض الجلسة العدادات المجمعة؛ النزاع وأدلته لرافعه أو منظم المباراة؛ كل الكتابات Backend-only | cross-user reads وarbitrary creates تفشل على Emulator |

**بوابة W1:** Functions unit + Auth/Firestore Emulator خضراء، وكل callable تشتق actor من Auth وتملك اختبار idempotency واختبار unauthorized.

### Wave 2 — فصل الخصوصية وسلامة الحساب `P0`

| التذكرة | التنفيذ | القبول |
| :--- | :--- | :--- |
| `ELR-IDN-201` (`V3-201`) | تنفيذ `ensurePlayerProfile`, `updateOwnPlayerProfile`, `setUsername` بقيم ووقت خادميين | لا يستطيع العميل إنشاء أو تعديل role/rating/counters/trust/createdAt |
| `ELR-IDN-202` (`V3-202`) | إضافة نموذج ومستودع `playerPublicProfiles` وتحويل public profile/search/leaderboard/roster/share identity إليه | لا توجد قراءة عامة من `players` داخل Flutter بعد الجرد الآلي |
| `ELR-IDN-203` (`V3-203`) | ناشر backfill إداري: preview افتراضي، تأكيد صريح لـ`el7reef-app/(default)`، توقف عند conflict، وتقرير anomalies | العدد يطابق اللاعبين، ولا يظهر أي حقل محظور في أي projection |
| `ELR-IDN-204` (`V3-204`) | ترتيب cutover: نشر Functions والإسقاطات → backfill/verify → إصدار Android الجديد → قفل `players` للمالك فقط | التطبيق الجديد يعمل؛ build قديم يفشل بأمان بدل كشف PII؛ لا rollback إلى rules مكشوفة |
| `ELR-IDN-205` (`V3-205`) | إبقاء Social المخفي خارج V1؛ block يستخدم Backend الحالي، ولا تُنقل arrays الخاصة إلى ملف عام | لا surface اجتماعية نصف عاملة ولا تسريب علاقات |

**بوابة W2:** Emulator attack matrix تمنع قراءة هاتف أو علاقات لاعب آخر، وتمنع forged initial profile، وتثبت search/leaderboard/public roster عبر projection فقط.

### Wave 3 — إغلاق UX المعروف `P1`

| التذكرة | التنفيذ | القبول |
| :--- | :--- | :--- |
| `ELR-UX-301` (`V3-301`) | التشكيلة: الصور القديمة لا تطابق `main`؛ يثبت build جديد أولًا. بعدها تصبح `visualSlots` مصدر الحقيقة و`visualBench` getter مشتقًا، وكل assign/swap/drag/formation/resize/reset/cancel تمر عبر invariant واحدة | قبل الحفظ دائمًا `pitchKeys ∩ benchKeys = ∅` واتحادهما يساوي اللاعبين النشطين، للمسجل والضيف و5/7/11 لاعبًا |
| `ELR-UX-302` (`V3-302`) | الشجرة: وضع الجولات يبقى افتراضي الهاتف؛ وضع الخريطة يخرج من `ListView/RefreshIndicator` ويملأ المساحة حتى لا يتنافس pan مع scroll | تحريك الخريطة لا يحرك الصفحة، وتفتح على المواجهة الحالية بمقياس مقروء |
| `ELR-UX-303` (`V3-303`) | شريط زجاجي واحد للخريطة: «الجولات، الحالي، نظرة عامة، الكأس»؛ العقد تعرض شعارًا/initials ونتيجة LTR وحالة؛ الضغط يحدد المسار ويفتح Sheet بها «افتح المباراة» | 360/600dp وRTL وtext scale 200% بلا تداخل؛ BYE والترجيح والبطل واضحون |
| `ELR-UX-304` (`V3-304`) | TalkBack يحول الشجرة لمسار خطي حسب الدور، والتصميم الدائري يُستخدم فقط في بطاقة مشاركة `Road to Cup` لا في التشغيل | التنقل السمعي يقرأ الدور والفريقين والنتيجة والحالة بترتيب مفهوم |

**بوابة W3:** goldens وwidget tests عند 360/600، ثم تحقق على `Pixel_4a_API_35_Lite` وProfile Mode لشجرة 32 فريقًا.

### Wave 4 — CI والاعتماديات `P0/P1`

| التذكرة | التنفيذ | القبول |
| :--- | :--- | :--- |
| `ELR-OPS-401` (`V3-401`) | إزالة تجاهل root `package-lock.json`، توليده ومراجعته تحت Node 20، وضبط `cache-dependency-path` على root وFunctions locks | `npm ci` و`npm --prefix functions ci` ينجحان من clean checkout، وإعادة package-lock-only لا تغير الملفين |
| `ELR-OPS-402` (`V3-402`) | إصلاح الملفات السبعة فوق budget باستخراج المسؤوليات مع characterization tests؛ لا رفع جماعي للأرقام | `npm run check:hotspots` أخضر، وأي baseline reconciliation استثنائي موثق لكل ملف لا كزيادة صامتة |
| `ELR-OPS-403` (`V3-403`) | تشغيل `npm audit --omit=dev --json` متصلًا بعد موافقة صريحة وحفظ artifact مؤرخ؛ audit root tooling منفصل | صفر High/Critical في production tree؛ كل Moderate لها fix أو استثناء بمالك وسبب وتاريخ انتهاء |
| `ELR-OPS-404` (`V3-404`) | تحديث Firebase dependencies بإصدارات متوافقة واختبار مسار Storage/delete-account؛ لا `npm audit fix --force` | Functions unit/emulator وحذف الحساب وStorage smoke خضراء |

**بوابة W4:** GitHub Actions الخضراء هي الدليل؛ لا يُقبل نجاح الجهاز المحلي وحده.

### Wave 5 — النشر وAndroid Closed Testing `P0`

| التذكرة | المالك | القبول |
| :--- | :---: | :--- |
| `ELR-REL-501` (`V3-501`) تفعيل Blaze وbudget alerts | OWNER | النشر مسموح ومراقب |
| `ELR-REL-502` (`V3-502`) backup ثم نشر Functions + rules + indexes المتوافقة في نافذة واحدة | ENG | post-deploy smoke لكل callable والقواعد، ولا fallback محلي للكتابات الحساسة |
| `ELR-REL-503` (`V3-503`) release/upload signing وPlay App Signing وتحديث SHA في Firebase وassetlinks | OWNER + ENG | Release AAB موقّع، package `com.el7reef.app` وTarget API 35+، وApp Links verified ببصمة Play |
| `ELR-REL-504` (`V3-504`) Android E2E بحسابين | QA | Google login → بطولة → فرق/ضيوف → groups/knockout → optional lineup → goals/MVP/penalties → approval → standings/bracket → Pride share → Claim |
| `ELR-REL-505` (`V3-505`) بطولة «كأس العالم 2026» بعد rules cutover | QA | تظهر كمميزة للمستخدم المسجل غير المنظم، وتفتح الفرق والمجموعات والمباريات والترتيب والشجرة بلا أدوات إدارة |
| `ELR-REL-506` (`V3-506`) وصول وأداء | QA | TalkBack، رفض الكاميرا، شبكة بطيئة، WhatsApp return، QR من جهاز ثان، text scale 200%، وp95 أقل من 16ms دون إطار دافئ فوق 32ms في التشكيلة والشجرة |
| `ELR-REL-507` (`V3-507`) Play readiness | OWNER | اعتماد الخصوصية والاحتفاظ وData Safety وحساب المراجع وتعليماته ومطابقتها للـAAB النهائي |

**بوابة W5:** لا blocker في المسار الكامل، ولا High/Critical، وrules audit النهائي 5/5، وCI خضراء، وRelease AAB مثبت من Closed Testing.

### Wave 6 — Pilot والتدرج

- يبدأ Pilot بمنظم فعلي وبطولة صغيرة، مع قياس زمن إنشاء البطولة، تسجيل خمسة أهداف وMVP، أول مشاركة، نجاح Claim، وفشل/إعادة المحاولة.
- تبقى `fan_voting_enabled` و`disputes_enabled` مغلقتين حتى نجاح سيناريوهاتهما بحسابين؛ ثم تفعيل 10%، فـ50%، فـ100% بعد استقرار الأعطال والـauthorization errors.
- لا يُسمى الرجوع من Android share sheet «مشاركة مكتملة».
- rollback يعطل flags أو يعيد Functions idempotently؛ لا يعيد قواعد تسمح بقراءة PII ولا يعيد callable التدقيق العامة.

---

## 9. مصفوفة التحقق الإلزامية

### Security وBackend

- forged audit entity/action/payload مرفوضة ولا تنشئ مستندًا.
- كل انتقال dispute ينجح بالكامل أو لا يكتب شيئًا، مع retry متزامن.
- vote مكرر أو من مشارك أو لحساب حديث أو مرشح مزور مرفوض.
- إنشاء player مزور بـrole/rating/createdAt/extra field مرفوض.
- مستخدم ثانٍ لا يقرأ phone أو friend/follow/block lists ولا evidence نزاع لا يخصه.
- نتيجة scoped rules audit هي `5/5` مع JSON جديد، وليس تقريرًا وصفيًا فقط.

### Flutter وUX

- `dart analyze lib/`&rlm;.
- `flutter test --no-pub` كامل، مع الاختبارات المركزة للتشكيلة والشجرة وAuth/public profiles.
- goldens عند 360 و600dp، RTL وtext scale 200%.
- لا player مكرر بين الملعب والبدلاء في أي draft state.
- لا scroll conflict في خريطة الشجرة، ومسار TalkBack خطي.

### Functions وCI

- `npm run test:functions`&rlm;.
- `npm run test:functions:emulator`&rlm;.
- `npm run test:rules:emulator`&rlm;.
- `npm run check:format:changed` و`check:architecture` و`check:indexes` و`check:hotspots`&rlm;.
- `npm ci` في root و`npm --prefix functions ci` من checkout نظيف.
- `git diff --check`&rlm;.

### Android

- لا أعمال iOS.
- API 24 وAPI 35 على الأقل، مع Light/Dark system دون وميض داكن رغم ثبات ثيم التطبيق الفاتح.
- جهاز/محاكي نظيف، حسابان، ونسخة من Play Closed Testing قبل قرار الإطلاق.

---

## 10. تعريف اكتمال Android V1

لا تُعلن V1 مكتملة إلا إذا كانت كل تذاكر `ELR-SEC-001` إلى `ELR-REL-507` المدرجة في موجات V1 إما `DONE` بدليل قابل للإعادة، أو ألغيت بقرار OWNER موثق يزيل القدرة من الإصدار. لا يكفي نجاح اختبارات محلية لتذكرة تتطلب نشرًا أو جهازًا أو Play Console.

الدليل النهائي يتكون من:

1. CI خضراء من commit الإصدار.
2. JSON تدقيق قواعد 5/5 وJSON dependency audit بلا High/Critical.
3. تقرير backfill يثبت صفر PII في `playerPublicProfiles`&rlm;.
4. Release AAB موقّع وبصمة Play App Links verified.
5. فيديو/لقطات وسجل نتائج لمسار Android E2E بالحسابين.
6. اعتماد Data Safety والسياسات وحساب المراجع.
7. Pilot فعلي بلا فقد بيانات أو كتابة نصف مكتملة أو blocker في حلقة البطولة والمشاركة والـClaim.

## 11. خارطة ما بعد V1

لا يبدأ أي بند في هذه الخارطة قبل إغلاق V1 وقراءة نتائج الـPilot. وجود البند هنا يحفظ القرار ولا يمنحه أولوية تنفيذ تلقائية.

| المعرف | الأفق | الحالة | الفكرة والقيمة | المالك | شرط إعادة الفتح | معيار القبول الأولي | المصدر القديم |
| :--- | :---: | :---: | :--- | :---: | :--- | :--- | :--- |
| `ELR-IDN-601` | `V1.1` | `PARKED` | توسيع هوية اللاعب بالقدم المفضلة ووسوم أسلوب اللعب ونبذة قصيرة | Product + ENG | نجاح V1 ووجود استخدام متكرر للبروفايل | حقول اختيارية موثقة وخصوصية واضحة وبطاقة لاعب حقيقية | `V2-045`, `V2-048` |
| `ELR-GROW-601` | `V1.1` | `PARKED` | بحث موحد للاعبين والفرق والبطولات وتوسيع الاستكشاف | Product + ENG | اكتمال الإسقاطات العامة الآمنة في `ELR-IDN-202` | نتائج ذات صلاحيات صحيحة، RTL، وحالات فارغة دون تسريب بيانات خاصة | `V2-049`, `V2-050` |
| `ELR-TEAM-601` | `V1.1` | `PARKED` | طلب انضمام يبدأه اللاعب ويقبله أو يرفضه قائد الفريق | Product + ENG | ثبات العضويات والصلاحيات في Pilot | lifecycle ذري، منع التكرار، وإشعار للطرفين | `V2-051` |
| `ELR-NOTIF-601` | `V1.1` | `PARKED` | نموذج ومستودع وصندوق وارد داخل التطبيق للدعوات وClaim والمباريات | Product + ENG | ثبات العقود التي ستنتج الإشعارات | Inbox حقيقي مع deep links وقراءة/أرشفة؛ Push مرحلة منفصلة | `V2-052`, `V2-053` |
| `ELR-IDN-602` | `V2` | `PARKED` | تاريخ الفرق والبطولات والمشاركات الرسمية وسمعة قابلة للتفسير | Product + ENG | توفر بيانات تشغيلية كافية بعد أكثر من Pilot | لا ratings مختلقة؛ كل إشارة قابلة للتفسير والاعتراض | `V2-046`, `V2-047` |
| `ELR-TEAM-602` | `V2` | `PARKED` | حساب إداري للأكاديميات يتابع عدة لاعبين وحضورهم ومشاركاتهم | Product | مقابلات مستخدمين تثبت الحاجة ونموذج صلاحيات معتمد | عزل تام بين الأكاديميات وموافقة واضحة على بيانات القُصّر | الرؤية التاريخية المسجلة في سجل التقاعد |
| `ELR-CORE-601` | `V1.1` | `PARKED` | تعميق حقيقة يوم المباراة: حضور، بداية، بدلاء، تبديلات ومن لعب فعلًا | Product + ENG | ثبات الحلقة الأساسية وطلب واقعي من المنظمين | سجل زمني للزوار والمسجلين دون جعل التشكيلة شرطًا للبدء | `V2-024..028` |

## 12. صندوق الأفكار الدائم

### 12.1 أفكار تحت الدراسة

| المعرف | الأفق | الحالة | الفكرة | سبب الحفظ | شرط الترقية إلى `READY` |
| :--- | :---: | :---: | :--- | :--- | :--- |
| `ELR-GROW-701` | `V2` | `INBOX` | مكتبة أعلام وأيقونات وشعارات افتراضية للبطولات والفرق | تقلل الاحتكاك وتدعم المشاركة | سياسة أصول وترخيص، أحجام Android، وتدفق اختيار قابل للوصول |
| `ELR-GROW-702` | `LATER` | `INBOX` | مصمم شعار بسيط مبني على قوالب وألوان ورموز معتمدة | يمنح الفرق هوية قابلة للتفاخر | تحديد نطاق V1 للمحرر، عقد تصدير، ومنع ادعاء ملكية أصول الغير |
| `ELR-GROW-703` | `V2` | `INBOX` | توسيع كتالوج بطاقات المفاخرة والفيديو والإنجازات | المشاركة محرك النمو الرئيسي | بيانات مؤكدة، ميزانية تصدير، حقوق الصوت، وقياس fallback |
| `ELR-UX-701` | `V2` | `INBOX` | شجرة دائرية جذابة كـRoad to Cup قابلة للمشاركة | الإبهار مناسب للملف المصدر أكثر من التشغيل اليومي | prototype مقروء للمشاركة دون استبدال الشجرة التشغيلية الخطية |
| `ELR-IDN-701` | `LATER` | `INBOX` | شارات إنجاز وسمعة أعمق مشتقة من البطولات | تدعم الفخر والهوية | تعريف مصادر حقيقة ومقاومة التلاعب والاعتراض |

### 12.2 أفكار مجمّدة

| المعرف | الأفق | الحالة | الفكرة | سبب التأجيل | شرط إعادة الفتح | المصادر القديمة |
| :--- | :---: | :---: | :--- | :--- | :--- | :--- |
| `ELR-FANT-001` | `LATER` | `PARKED` | Fantasy lifecycle والانتقالات والـchips والتسوية والإدارة | لا تخدم إطلاق Tournament Ego V1 وتنافس النواة على الوقت | استقرار V1 وقرار Product جديد وADR لهوية المنافسة | `IMP-001..035`, `V2-037..040` |
| `ELR-SOC-001` | `LATER` | `PARKED` | Feed اجتماعي متقدم وعلاقات ومتابعة | خطر خصوصية وسطح واسع بلا حاجة V1 | اكتمال الإسقاط العام وسياسة الخصوصية وقياس retention | Epic Social القديم |
| `ELR-SOC-002` | `LATER` | `PARKED` | محادثات داخل التطبيق | خارج الوعد الحالي ويحتاج إشرافًا وسلامة إضافيين | إثبات حاجة لا يحلها WhatsApp وخطة moderation | الرؤية القديمة |
| `ELR-OPS-701` | `LATER` | `PARKED` | تجريد Firebase Auth وStorage خلف Provider interfaces | abstraction مبكرة بلا مزود ثانٍ | مزود ثانٍ أو عائق اختبار موثق | `V2-054`, `V2-055` |
| `ELR-CORE-701` | `LATER` | `PARKED` | عقود خرائط ومصادر بيانات مباريات خارجية | لا توجد ميزة حالية تستهلكها | دخول الخرائط أو البيانات الخارجية في نطاق معتمد | `V2-056` |
| `ELR-BIZ-001` | `LATER` | `PARKED` | اكتشاف وحجز الملاعب | منتج وتشغيل ومدفوعات مختلف | بحث سوق ونطاق منفصل وموافقة Product | الرؤية القديمة |
| `ELR-BIZ-002` | `LATER` | `PARKED` | اشتراكات أو رعاية أو Monetization | النمو والثقة لم يُثبتا بعد | بيانات استخدام مستقرة ونموذج قيمة عادل | الرؤية القديمة |
| `ELR-OPS-702` | `LATER` | `PARKED` | تحويل arrays القديمة إلى عضويات مستقلة إضافية | Migration بلا ضغط حقيقي يزيد الخطر | ضغط استعلام أو صلاحيات أو حجم مثبت | `V2-042` |

## 13. سجل القرارات المرفوضة والثابتة

| المعرف | الحالة | القرار | السبب | شرط إعادة النظر |
| :--- | :---: | :--- | :--- | :--- |
| `ELR-FANT-901` | `REJECTED` | اعتبار Fantasy المسار التالي أو جزءًا من V1 | تشتت عن البطولة والمشاركة وClaim | قرار Product بعد إطلاق V1، لا حالة Todo قديمة |
| `ELR-CORE-901` | `REJECTED` | منع بدء المباراة عند غياب التشكيلة | يخالف واقع الشارع والقرار الحالي والاختبارات | لا يعاد فتحه؛ التشكيلة تظل نudge اختيارية |
| `ELR-CORE-902` | `REJECTED` | إنشاء `CompetitionPlayer` أو `TournamentAthlete` مكرر الآن | يكرر `ParticipantRef` الحالي ويهدد الإحصائيات | ADR فقط عند عودة Fantasy وبحاجة غير محلولة بالعقد الحالي |
| `ELR-SEC-901` | `REJECTED` | اعتبار حدث Audit مرسل من العميل سجلًا رسميًا | العميل لا يملك سلطة تحديد الفعل والكيان الرسمي | لا يعاد فتحه |
| `ELR-CORE-903` | `REJECTED` | استخدام `player_stats` بدل `MatchEvent` كمصدر حقيقة | يكسر الزوار ويشتت الحقيقة | لا يعاد فتحه |
| `ELR-UX-901` | `REJECTED` | واجهات Placeholder أو تحليل تكتيكي أو نجوم وأرقام تقديرية | ميزات وهمية تقلل الثقة | لا يعاد فتحه دون مصدر بيانات حقيقي |
| `ELR-OPS-901` | `REJECTED` | اعتبار `Todo/In Progress` القديمة حالة حية | الحالات القديمة لا تثبت واقع الكود | التحقق من الكود والاختبارات ثم إنشاء تذكرة جديدة |
| `ELR-OPS-902` | `REJECTED` | افتراض أن بيانات الإنتاج قابلة للمسح أو أن Migration تؤجل تلقائيًا | توجد حسابات وبطولة عامة وبيانات مستخدمين | كل انتقال Preview وVerify وRollback وموافقة صريحة |
| `ELR-REL-901` | `REJECTED` | إدخال iOS في خارطة الإصدار الحالية | القرار هو Android أولًا | قرار Product مستقل بعد إطلاق Android؛ لا تذاكر iOS هنا |

## 14. سجل القدرات المنجزة

هذه البنود تمنع إعادة فتح أعمال قديمة بوصفها ناقصة، مع إبقاء اختبارات الانحدار جزءًا من أي تغيير لاحق.

| المعرف | الحالة | القدرة المثبتة | الدليل أو الحارس |
| :--- | :---: | :--- | :--- |
| `ELR-CORE-801` | `DONE` | بطولة بفرق ولاعبين مسجلين وزوار وتوليد مجموعات وإقصائيات | خدمات البطولة وتقارير TOS التاريخية واختبارات المجال |
| `ELR-CORE-802` | `DONE` | أهداف وMVP للمسجل والزائر و`matchSidePlayer` عبر أحداث المباراة | تقارير Sprint 1 وWave 2 واختبارات MatchEvent |
| `ELR-CORE-803` | `DONE` | بدء المباراة دون تشكيل إلزامي | اختبار صريح لمسار البداية؛ يحميه القرار `ELR-CORE-901` |
| `ELR-GROW-801` | `DONE` | بطاقات مفاخرة أساسية وروابط Claim ومشاركة | تقارير Sprint 1 و2؛ التوسعات المستقبلية مستقلة |
| `ELR-GROW-802` | `DONE` | بطولة كأس العالم 2026 عامة ومميزة للحسابات المسجلة | الناشر والتحقق وحساب الضيف؛ إعادة التحقق في `ELR-REL-505` بعد rules cutover |
| `ELR-UX-801` | `DONE` | أساس Daylight Street Glass والثيم الفاتح | `DESIGN.md` واختبارات التصميم الحالية |
| `ELR-UX-802` | `DONE` | إعادة تصميم التشكيلة السابقة | خطة 15 التاريخية؛ مشكلة تكرار draft الجديدة مستقلة في `ELR-UX-301` |

## 15. الحوكمة وسجل التقدم

### 15.1 دورة حياة الفكرة

1. تسجل الفكرة أولًا كـ`INBOX` بوصف مختصر ومصدرها.
2. يقرر Product الأفق أو الرفض أو الدمج مع فكرة قائمة.
3. لا تصبح `READY` قبل Definition of Ready والأمن والبيانات والاختبارات.
4. تنتقل تذكرة محددة فقط إلى `ACTIVE` عند بدء التنفيذ.
5. لا تصبح `DONE` دون دليل قابل للإعادة؛ ويحدث صفها وتاريخ المراجعة في التغيير نفسه.

### 15.2 قواعد منع تفرع الخطط

- لا ينشأ ملف Roadmap أو Backlog أو Master جديد؛ تضاف الفكرة أو التذكرة هنا.
- يمكن إنشاء Runbook أو ADR أو Audit متخصص، لكنه يعلن صراحة أنه ليس خطة نشطة ويعود بمعرّف لهذه الوثيقة.
- لا تنسخ المواصفات الطويلة هنا؛ تلخص النتيجة ويرتبط الدليل المتخصص.
- عند تعارض وثيقة قديمة مع الكود، يحكم الكود والاختبار الحاليان وتفتح تذكرة لتصحيح الوثيقة.
- يراجع المرجع عند كل انتقال حالة، وبعد كل Pilot، وقبل أي قرار إصدار.

### 15.3 سجل التقدم الحالي

| التاريخ | العمل | الحالة | الدليل |
| :--- | :--- | :---: | :--- |
| 2026-07-28 | تدقيق الأمن وCI وتكوين خطة الإغلاق | `DONE` | `docs/audits/2026-07-28_v1_security_completion_audit.json`&rlm; |
| 2026-07-28 | حفظ الحالة قبل توحيد الخطط | `DONE` | commit `7eb4ece` وtag محلي `docs-pre-consolidation-2026-07-28`&rlm; |
| 2026-07-29 | إنشاء المرجع الرئيسي وصندوق الأفكار وتقاعد الخطط المتضاربة | `DONE` | المرجع `5181774`، وcommit التقاعد `1aaf43e`، وحارس التوثيق واختباراته |

### 15.4 سجل المصادر

يحفظ `docs/archive/PLANNING_RETIREMENT_MANIFEST.md`&rlm; الـCrosswalk الكامل لكل خطة متقاعدة: المسار وGit blob وآخر commit والقرار والمعرفات الجديدة وcommit الحذف. يسمح Git tag السابق باستعادة النص الكامل، لذلك لا تحفظ نسخ خطط موازية داخل Archive.

</div>
